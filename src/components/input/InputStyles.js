import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container:{
    marginLeft:10,
    marginRight:10,
    borderBottomWidth:1
  },
  title:{
    padding:10
  }
})

export default styles